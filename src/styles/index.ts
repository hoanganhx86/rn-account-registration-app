import * as colors from './colors';
import * as fonts from './fonts';
import * as sizes from './sizes';
import * as types from './types';
import {sharedStyles} from './shared';

export {colors, fonts, types, sharedStyles, sizes};
